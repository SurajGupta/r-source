#!/bin/sh

helpdir=../../help

for dir in $*
do
	files="`ls $dir | sed /CVS/d`"
	for i in $files
	do
		doc2ms $dir/$i | nroff -ms 2> /dev/null | ./help.pretty | sed 's/^/	/' >  $helpdir/$i
		echo $i
	done
done
