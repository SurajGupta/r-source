TITLE(is.finite @@ Which Elements are Finite?)
USAGE(is.finite(x))
ALIAS(finite)
DESCRIPTION(
LANG(is.finite) returns a vector of the same length as LANG(x)
the jth element of which is LANG(TRUE) if the jth element of LANG(x) is
finite. 
PARA
At present, IEEE arithmetic is not supported and this is
simply the same as LANG(!is.na(x)).
)
