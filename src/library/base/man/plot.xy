TITLE(plot.xy @@ Basic Internal Plot Function)
USAGE(
plot.xy(xy, type, pch=1, lty="solid", col=par("fg"), bg=NA, cex=1, DOTS)
)
ALIAS(plot.xy)
ARGUMENTS(
ARG(xy @@ A four-element list as results from LANG(LINK(xy.coords)(..)).)
ARG(type @@ 1 character code )
ARG(pch @@ character or integer code for kind of points )
ARG(lty @@ line type code )
ARG(col @@ color code )
ARG(bg @@ background color )
ARG(cex @@ character expansion )
ARG(DOTS @@ further graphical parameters )
)
DESCRIPTION(
This is BOLD(the) internal function that does the basic plotting of points
and lines.
Usually, one should rather use the higher level functions instead
and refer to their help pages for explanation of the arguments.
)
SEEALSO(
LANG(LINK(plot)), LANG(LINK(plot.default)), LANG(LINK(points)), LANG(LINK(lines)).
)
EXAMPLES(
points.default # just calling "plot.xy(xy.coords(x, y), type = "p", ......)"
)
