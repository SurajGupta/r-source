TITLE(storage.mode @@ The Storage Mode of an Object)
USAGE(
storage.mode(x)
)
ALIAS(storage.mode)
DESCRIPTION(
LANG(storage.mode) provides a means of determining the type or
storage mode of an object.  LANG(storage.mode) returns a character
string giving the (storage) mode of the object.
)
SEEALSO(
LANG(LINK(mode)), LANG(LINK(typeof)).
)
