TITLE(on.exit @@ Function Exit Code)
USAGE(
on.exit(expr)
)
ALIAS(on.exit)
ARGUMENTS(
ARG(expr @@ an expression to be executed.)
)
DESCRIPTION(
LANG(on.exit) records the expression given as
its argument as needing to be executed when
then current function exits (either naturally or as the
result of an error).
This is useful for resetting graphical parameters or
performing other cleanup actions.
)
EXAMPLES(
opar <- par(mai=c(1,1,1,1))
on.exit(par(opar))
)
