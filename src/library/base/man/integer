TITLE(integer @@ Integer Vectors)
USAGE(
integer(n=0)
BLANK
as.integer(x)
is.integer(x)
)
ALIAS(integer)
ALIAS(as.integer)
ALIAS(is.integer)
VALUE(
This function creates a integer vector of the specified length.
Each element of the vector is equal to LANG(0).
Integer vectors exist so that data can be passed to C or Fortran
code which expects them.
PARA
LANG(as.integer) attempts to coerce its argument to be of
integer type.
PARA
LANG(is.integer) returns LANG(TRUE) or LANG(FALSE)
depending on whether its argument is of integer type or not.
)
