TITLE(frame @@ Start a New Plot Frame)
USAGE(
frame()
)
ALIAS(frame)
VALUE(
The function is invoked for its side effect which is to
cause the completion of plotting in the current plot
and an advance to a new graphics frame.
This is useful for skipping plots when a multi-figure region is in use.
)
