.GlobalEnv <- globalenv()
T <- TRUE
F <- FALSE
version <- Version()
.Machine <- Machine()
help<-function(...)
                cat("Sorry, no help is available (yet).\n")
options(na.action="na.omit")

.First <- function() windows()

